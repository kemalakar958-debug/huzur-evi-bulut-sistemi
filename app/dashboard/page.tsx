'use client';
import {useEffect,useState} from 'react';
import Shell from '@/components/Shell';
import {supabase} from '@/lib/supabaseClient';
export default function DashboardPage(){const[stats,setStats]=useState({facilities:0,patients:0,meds:0,depot:0,requests:0});useEffect(()=>{loadStats();const c=supabase.channel('dashboard-realtime').on('postgres_changes',{event:'*',schema:'public'},()=>loadStats()).subscribe();return()=>{supabase.removeChannel(c)}},[]);async function countTable(t:string){const{count}=await supabase.from(t).select('*',{count:'exact',head:true});return count||0}async function loadStats(){const[facilities,patients,meds,depot,requests]=await Promise.all([countTable('facilities'),countTable('patients'),countTable('medications'),countTable('depot_items'),countTable('stock_requests')]);setStats({facilities,patients,meds,depot,requests})}return <Shell><div className="hero"><h2>Bulut Klinik Yönetim Paneli</h2><p>Telefon, MacBook ve bilgisayarlarda aynı anda çalışacak çoklu kullanıcı sisteminin başlangıç sürümü.</p></div><div className="kpiGrid"><div className="kpi"><span>Kurum</span><strong>{stats.facilities}</strong></div><div className="kpi"><span>Hasta</span><strong>{stats.patients}</strong></div><div className="kpi"><span>Aktif İlaç</span><strong>{stats.meds}</strong></div><div className="kpi"><span>Depo Ürünü</span><strong>{stats.depot}</strong></div></div><div className="panel"><div className="panelHead"><div><h2>Gerçek Zamanlı Sistem</h2><p>Başka bilgisayarda yapılan değişiklikler otomatik güncellenir.</p></div><span className="pill ok">Realtime</span></div><div className="notice">Önce Supabase SQL dosyasını çalıştırıp .env.local dosyasını doldurmalısın.</div></div></Shell>}
